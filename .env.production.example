# Variáveis lidas pelo docker-compose.prod.yml (interpolação do serviço "db").
# Na VPS, copie para ".env" (mesma pasta do compose) e preencha. NÃO versionar o .env real.
DB_USER=postgres
DB_DATABASE=audisped_db
DB_PASSWORD=TROQUE_POR_SENHA_FORTE
