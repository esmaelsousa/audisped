# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Environment
.env
.env.bak*
*.bak
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
*.log
backend/*.log
backend/exceptions.log
backend/audisped-backend.log

# Database dumps / Backups
*.sql
*.backup
*.sql.gz

# Python (venv)
venv/
venv_pdf/
__pycache__/
*.py[cod]

# Project Specific (Sensitive Data)
backend/uploads/
speds/
backend/tests/golden/
SEGURANCA_AUDITORIA_VALIDADA.md
# Docs derivados do datadir de terceiro (leiaute forense) — NÃO versionar (procedência/sigilo).
# O leiaute para o produto deve ser semeado do Guia Prático / Ato COTEPE (fonte pública).
REGRAS_PVA.md
# Relatórios/auditorias específicos de CLIENTE (sigilo fiscal/LGPD) — não versionar.
RELATORIO_AUDITORIA_PRECO_BOM_II.md
RELATORIO_DE_PARA_PVA_vs_BD.md
# Plano que cita datadir de terceiro — manter local (já anonimizado, mas fora do repo).
PLANO_CATALOGO_REGRAS_SPED.md
# Backup antigo commitado por engano (contém código + dados de cliente).
backup_20260328_205837/
*.txt
*.pdf
!package.json
!package-lock.json

# IDEs
.vscode/
.idea/
.DS_Store

# Brain / Antigravity artifacts (if inside workspace)
.gemini/
antigravity-awesome-skills/
frontend/dist/

# Cache de compilacao Node (nao versionar)
node-compile-cache/
